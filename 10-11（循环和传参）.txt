|-------关于for循环和while循环

    1.for循环
        for (1.初始条件;2.判断条件;3.自增){
          // code
         }
    2. while循环的格式
         （1）初始条件 var i = 0 ;
         （2）while( i<3 ){//判断条件
                //code
                i++;//自增操作，注意别忘了，否则会无限循环
            }
 遇到的一些问题：
    1.什么时候使用for循环？什么时候使用while循环？
         循环次数确定的时候用for循环
         循环次数不确定的时候用while循环

    建议使用for循环---》可读性更高
    
    2. 循环里面加事件，事件里面取不到i值  事件里面的i值是循环结束后的i
       可以给它先加index，用index取到当时的i  
  或者是    用function参数形式封装  同时当前循环传入参数i

    3. 循环里面套循环，两个循环变量名不能一样。但是如果循环里面加事件，事件里面再加循环，两个循环变量名可以一样

 循环举例：（只要操作一组，多个元素的时候就要用到循环）
      点击一组div变色步骤：
         1.先把所有的div颜色都变成默认的颜色
         2.再把当前的div的背景色变为想要的颜色

|-----------选项卡
     
     1. 按钮和内容的个数是对应[相等]的
     2. 当前选中的按钮有选中状态，其他按钮没有选中状态
     3. 和当前选中的按钮对应的元素显示，其他的元素隐藏
   选项卡步骤：
      1.先做按钮：
          （1）先清空所有按钮的className
			for(var i = 0; i < aBtn.length; i++){
				aBtn[i].className = '';
			}
	  （2）当前点击的按钮加选中的状态[className];
			this.className = 'active';

      2.把按钮和div关联起来;
	  （1）隐藏所有的元素
			for(var i = 0; i < aDiv.length; i++){
				aDiv[i].style.display = 'none';
			}
	  （2）和当前按钮对应的div显示;
			aDiv[this.index].style.display = 'block';

  自定义属性：非系统自带的属性;低版本浏览器[IE7/8]可以取到值;高版本取不到值;会报出undefined;   
       
高版本浏览器[非IE7/8]的加载过程：
	1.先加载系统自带的属性和标签;
	2.过滤自定义的标签和属性;
	3.加载JS;



|----------参数传递

    传参的格式:
	function show(a,b){//.1定义一个参数
		alert(a+b);2.使用一个参数
	}
	show(1,2);3.传递一个参数


  



  当事件由一个参数传入时（比如onclick）不能用 .style.参数  而应该用.style[参数]
   因为 .不能操作变量,操作变量用[];





    